let name = 'chris'; //string literal aka a string
let age = 33; //number literal 
let isApproved = true; //boolean literal
let firstName = undefined; //if not initialize it will be set to undefined
let selectedColour = null;

// using the console we can see the variable type using typeof "Var"
// number can be a float or int
// var types can be changed dynamically i.e name = 1 
// null type is an object   